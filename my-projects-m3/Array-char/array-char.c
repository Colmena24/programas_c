#include <stdio.h>
#include <stdlib.h>
#include <diprog.h>

// FUNCION PRINCIPAL

int main(void) {

    string s = "ja!";
    /*printf("%c %c %c \n", s[0], s[1], s[2]);*/
    printf("%i %i %i \n", s[0], s[1], s[2]);
}