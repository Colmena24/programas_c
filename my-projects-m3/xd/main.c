#include <stdio.h> 
#include <diprog.h>  

int main(int argc, string argv[]) {    
    for (int i = 1; i < argc; i++) {        
        printf("argv[%i] = %s\n", i, argv[i]);
            } 
            } 